import { CategoryFilter } from "./actionTypes";
import { initialState } from "./initialState";



const reducer = (state=initialState, action) => {

    switch (action.type) {
        case CategoryFilter:
            
            break;
    
        default:
            break;
    }

}

export default reducer;